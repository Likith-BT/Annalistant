Streamlit Analytics Assistant (final)

How to run:
1. (Optional) Create virtualenv:
   python -m venv venv
   source venv/bin/activate  # mac/linux
   venv\Scripts\activate     # windows
2. Install deps:
   pip install -r requirements.txt
3. Run app:
   streamlit run app.py
